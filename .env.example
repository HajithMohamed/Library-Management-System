# ==========================================
# Library Management System - Environment Configuration
# ==========================================
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update all values marked with "CHANGE_THIS" or "your_*"
# 3. Never commit .env to version control (already in .gitignore)
#
# SECURITY NOTE:
# - Use strong, unique passwords for production
# - Enable 2FA on all external services
# - Rotate credentials every 90 days
# ==========================================

# ==========================================
# APPLICATION SETTINGS
# ==========================================
# Timezone for the application (PHP date functions)
# See: https://www.php.net/manual/en/timezones.php
TZ=Asia/Kolkata

# Web server port (default: 8080)
WEB_PORT=8080

# PHPMyAdmin web interface port (default: 8081)
PHPMYADMIN_PORT=8081

# Debug mode - CRITICAL: Set to "false" in production!
# When enabled, displays detailed error messages
APP_DEBUG=true

# OTP expiration time in minutes (for email verification)
OTP_EXPIRY_MINUTES=15

# ==========================================
# DATABASE CONFIGURATION
# ==========================================
# MySQL root password (for database administration)
# PRODUCTION: Use a strong password (16+ characters, mixed case, numbers, symbols)
MYSQL_ROOT_PASSWORD=CHANGE_THIS_ROOT_PASSWORD

# Application database name
MYSQL_DATABASE=integrated_library_system

# Application database user (non-root user for security)
MYSQL_USER=library_user

# Application database user password
# PRODUCTION: Use a strong, unique password
MYSQL_PASSWORD=CHANGE_THIS_DB_PASSWORD

# ==========================================
# APPLICATION DATABASE SETTINGS (for PHP)
# ==========================================
# Database host (use 'db' for Docker, 'localhost' for local development)
DB_HOST=db

# Database port (default MySQL port: 3306)
DB_PORT=3306

# Database username (should match MYSQL_USER above)
DB_USER=library_user

# Database password (should match MYSQL_PASSWORD above)
DB_PASSWORD=CHANGE_THIS_DB_PASSWORD

# Database name (should match MYSQL_DATABASE above)
DB_NAME=integrated_library_system

# ==========================================
# ADMIN CONFIGURATION
# ==========================================
# Admin registration code (required when creating admin accounts)
# PRODUCTION: Use a strong, unique code and share only with authorized admins
ADMIN_CODE=CHANGE_THIS_ADMIN_CODE

# ==========================================
# EMAIL CONFIGURATION (SMTP)
# ==========================================
# SMTP server hostname
# Gmail: smtp.gmail.com
# Outlook: smtp-mail.outlook.com
# Yahoo: smtp.mail.yahoo.com
SMTP_HOST=smtp.gmail.com

# SMTP server port
# TLS (recommended): 587
# SSL: 465
SMTP_PORT=587

# SMTP username (usually your email address)
SMTP_USERNAME=your_email@gmail.com

# SMTP password or app password
# Gmail users: Generate an App Password at https://myaccount.google.com/apppasswords
# Requires 2FA to be enabled on your Google account
SMTP_PASSWORD=your_app_password

# SMTP encryption method (tls or ssl)
SMTP_ENCRYPTION=tls

# Email address to send from (can be same as SMTP_USERNAME)
SMTP_FROM_EMAIL=your_email@gmail.com

# Display name for sent emails
SMTP_FROM_NAME=Library Management System University of Ruhuna

# ==========================================
# CLOUDINARY CONFIGURATION (Image Upload Service)
# ==========================================
# Used for book cover images and e-resource thumbnails
# Sign up for free at: https://cloudinary.com/users/register/free
# Find credentials at: https://console.cloudinary.com/console

# Your Cloudinary cloud name
CLOUDINARY_CLOUD_NAME=your_cloud_name

# Your Cloudinary API key
CLOUDINARY_API_KEY=your_api_key

# Your Cloudinary API secret
CLOUDINARY_API_SECRET=your_api_secret

# ==========================================
# SMS GATEWAY CONFIGURATION (Optional)
# ==========================================
# Leave blank to disable SMS notifications
# If enabled, configure with your SMS provider's details

# SMS provider API endpoint URL
SMS_API_URL=

# SMS provider API key / authentication token
SMS_API_KEY=

# SMS sender ID (registered with your SMS provider)
SMS_SENDER_ID=

# ==========================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ==========================================
# Before deploying to production, ensure:
# [ ] APP_DEBUG is set to "false"
# [ ] All passwords are strong and unique
# [ ] ADMIN_CODE is changed from default
# [ ] SMTP credentials are configured and tested
# [ ] Cloudinary credentials are configured (if using image uploads)
# [ ] File permissions are set correctly (see SECURITY.md)
# [ ] HTTPS is enabled with valid SSL certificate
# [ ] Database backups are configured
# [ ] Error logging is configured (see logs/ directory)
# [ ] .env file is not committed to version control
# [ ] All database migrations are applied (php migrate.php status)
# [ ] Seeder data is removed or substituted with real data
# ==========================================